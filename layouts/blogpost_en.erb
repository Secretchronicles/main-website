<!DOCTYPE HTML>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <title><%= @item[:title] %> | Secretchronicles Blog</title>
    <link rel="stylesheet" href="/stylesheets/default.css"/>
    <link rel="alternate" type="application/atom+xml" title="Feed" href="/en/feed.atom"/>
    <meta name="generator" content="nanoc <%= Nanoc::VERSION %>">
  </head>
  <body>
  <div id="page">

    <%= render "_head_en" %>

    <div id="main">
      <h1><%= @item[:title] %></h1>

      <p class="summary">
	<i>Written by <b><%= @item[:author] %></b> on <b><%= @item[:date].strftime("%Y-%m-%d") %></b>.

        <% if @item[:translator] && !@item[:translator].empty? %>
          <br/>
          Translated by <i><%= @item[:translator] %></i>.
        <% end %>

	<br/>

	<% if @item[:summary] && !@item[:summary].empty? %>
	  <br/>
	  <b><%= @item[:summary] %></b>
        <% end %>
        </i>
      </p>
      
      <%= yield %>
    </div>

  </div>

    <%= render "_foot" %>

  </body>
</html>
