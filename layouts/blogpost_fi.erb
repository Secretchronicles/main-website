<!DOCTYPE HTML>
<html lang="fi">
  <head>
    <meta charset="utf-8">
    <title><%= @item[:title] %> | Secretchronicles Blogi</title>
    <link rel="stylesheet" href="/stylesheets/default.css"/>
    <link rel="alternate" type="application/atom+xml" title="Feed" href="/fi/feed.atom"/>
    <meta name="generator" content="nanoc <%= Nanoc::VERSION %>">
  </head>
  <body>
  <div id="page">

    <%= render "_head_fi" %>

    <div id="main">
      <h1><%= @item[:title] %></h1>

      <p class="post-info">
	Kirjoittanut <%= @item[:author] %> <%= @item[:date].strftime("%Y-%m-%d") %>.

        <% if @item[:translator] && !@item[:translator].empty? %>
	  <br/>
	  Suomentanut <%= @item[:translator] %>.
	<% end %>
      </p>

      <%= yield %>
    </div>

  </div>

    <%= render "_foot" %>
  </body>
</html>
